#include<iostream.h>
#include<conio.h>
void main()
{
	int m[3],i;
	clrscr();
	for(i=0;i<3;i++)
	{
		cout<<"enter subject"<<" "<<i+1<<" "<<"marks:";
		cin>>m[i];
	}
	if(m[0]>=40&&m[1]>=40&&m[2]>=40)
	{
		cout<<"pass";
	}
	if(m[0]>=40&&m[1]>=40&&m[2]<40)
		{
		cout<<"atkt";
	}
	if(m[0]<40&&m[1]>=40&&m[2]>=40)
		{
		cout<<"atkt";
	}
	if(m[0]>=40&&m[1]<40&&m[2]>=40)
	{
		cout<<"atkt";
	}
	if(m[0]<40&&m[1]<40&&m[2]<40)
	{
		cout<<"fail";
	}
	getch();
}
